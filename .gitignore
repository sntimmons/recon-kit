# ── Virtual environments ──────────────────────────────────────────────────────
venv/

# Root-level venv artifacts (venv was initialized at project root)
Include/
Lib/
Scripts/
pyvenv.cfg
etc/

# ── Python bytecode ───────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo

# ── Sensitive data — real employee records, NEVER commit ─────────────────────
data/
inputs/
outputs/

# ── Generated run folders ────────────────────────────────────────────────────
runs/
dashboard_runs/
share/

# ── Generated database files ──────────────────────────────────────────────────
*.db

# ── Generated audit artifacts ─────────────────────────────────────────────────
audit/audit_runs/
audit/audit_archive/
audit/corrections/out/
audit/exports/out/
audit/summary/charts/

# Generated CSVs in audit (derived from real data)
audit/*.csv
audit/summary/*.csv
audit/summary/*.xlsx
audit/summary/*.html
audit/summary/*.md
audit/ui/ui_pairs.csv

# Review output folders (generated)
review/
review_duplicate_worker_id_new/
review_duplicate_worker_id_old/
duplicate_worker_id_new/

# Root-level generated/temp files
phase3_scorecard.csv
tmp_*.py

# ── Site downloads (generated from real data) ────────────────────────────────
site/downloads/

# ── Local Claude agent settings ───────────────────────────────────────────────
.claude/

# ── Generated summary JSONs ───────────────────────────────────────────────────
audit/summary/sanity_gate.json
audit/summary/sanity_results.json

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
.env.*

# ── OS / editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/

# ── Node (if dashboard ever gets one) ────────────────────────────────────────
node_modules/

# ── Keep synthetic test-pack CSVs (safe — generated by Faker) ────────────────
!test_packs/**/*.csv
.tools/
